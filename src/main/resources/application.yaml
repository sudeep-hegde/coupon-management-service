spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/coupon
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

#  sql:
#    init:
#      mode: always

server:
  port: 8080